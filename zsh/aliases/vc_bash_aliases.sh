# editing often used files using Visual Studio Code

# I have all the shell-config stored in the ~/bin/ folder
alias vcshell='code -r $HOME/.config/shell-config.code-workspace'
alias vcg='code ~/.gitconfig'
alias vczsh='code ~/.zshrc'
